{
  "name": "pojo2xml",
  "version": "1.3.0",
  "description": "Convert plain old javascript objects (POJOs) to XML.  Fast.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha test/index.js",
    "release": "git push && git tag v$(jq -r .version package.json) && git push --tags && npm publish"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git://github.com/alxndrsn/pojo2xml.git"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.0"
  }
}
